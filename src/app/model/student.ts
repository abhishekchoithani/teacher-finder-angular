interface User {
    email: string;
    // Other properties...
  }
  
  // Example usage
  const user: User = {
    email: 'example@example.com',
    // Other properties...
  };
  
  // Accessing the 'email' property
  console.log(user.email);